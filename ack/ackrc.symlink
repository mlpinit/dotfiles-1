# Case insensitive and recursive by default
-ri

# follow symlinks
--follow

# Set pager to $PAGER
--pager=$PAGER

# Ignore directories
--ignore-dir=.git
--ignore-dir=node_modules
--ignore-dir=vendor

--type-set=coffee=.coffee
